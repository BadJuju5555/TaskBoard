<Window x:Class="TaskBoard.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="450"
        Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="lblMessage" Text="Message:"/>
        <TextBox x:Name="txtInput" Grid.Row="1" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="5" IsDefault="True" Click="OK_Click"/>
            <Button Content="Abbrechen" Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
